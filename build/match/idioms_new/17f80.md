# 0x10017F80 BrFadeDrawSprite — 2-arg globals DL emit

Not MATCH: 529/529, REGNORM 0+0, RAW 0+0, **2 diffs**.

## Orig shape

cdecl `(const uint32_t *pRecs, float alpha)` — no BrFadeState *.
Cursor `DAT_106e7710`, rectIdx `DAT_106ec798`, otherModeH `DAT_106e7718`.

Early-out `test ah,1 / jne ret` after fcomp 0.1f (`!(alpha >= 0.1f)`).
Cap `alpha > 0.7f` overwrites the arg slot `[esp+0x14] = 0x3f333333`.

Each emit is `{ BrGfxWords *p = DAT_106e7710++; p->w0=C; p->w1=A; }`
(`add reg, 8`). Combine is
`BrRdpSetCombineLERP(DAT_106e7710++, 0,0,0, t1, 0,0,0, t5, 0,0,0, t9, 0,0,0, t13)`
(17 cdecl args, `add esp, 0x44`). Immediates hoist backward into the
preceding E7/BA/B9 stores. First combine all `0x3EB`; second
t1=t5=`0x3EB`, t9=t13=`0x3E8`.

`(int)(alpha * 255.0f)` is `__ftol` (`call 0x10074560`).
`br16_ftol((double)...)` emits `fstp qword`.

Rect records are 88 B (`idx*11` then `[base+eax*8+disp]`). Orig loads
`[+0xC], [+4], [+8]` into esi/edi/ebx, `lea` base, then `[eax]` for +0.

## Residue

`mov esi, [edx+eax*8+0xc]; mov edi, [edx+eax*8+4]` vs swapped +4 / +0xC.
Then both `add esi, edi` (same sum). Commutative `y1+y0` — /O2 loads y0
first. `s = y1; s += y0` and `volatile` field order both failed (volatile
dropped the scaled-index form, 139 diffs). Same class as `|` operand
canonicalization.

Proven against orig 0x10017F80.bin /O2.
