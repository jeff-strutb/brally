# 0x1003CD60 BrOpt3810 — goto-tail leave_host, C++ thiscall 6A4A0

C cdecl lobby handler, 490 B, no EH. Not MATCH (2 diffs in C++).

## Layout (proven)

`if (AA2894 == 0) goto check; if (A9D000 != 0) goto psub; goto leave_host;`
then `psub:` falls through into `check:`. Nested
`if (AA2894) { if (!A9D000) { leave; return; } }` inlines leave at the
top (`jne` over it). Orig is `je check` (short) then `je leave_host`
(far `0F 84`, target +0x174).

leave_host reloads pGame from `[esp+0x10]`, not `push edi`.

## Thiscall

- GameSub slot+0x18: `push 0; mov ecx,this; mov edx,[ecx]; call [edx+0x18]`
  (`s6(0)` in C++, or `__fastcall` with vtbl as edx).
- Phase slot+0x1C: no stack arg, `mov edx,[ecx]; call [edx+0x1c]`.
  Virtual `pObj->f1C()` emits `mov eax,[ecx]; call [eax+0x1c]` on the
  AA2890 site (eax free after 46400). Force edx with
  `__fastcall (this, vtbl)` / `vt[7](pObj, vt)` there. leave_host's
  virtual already uses edx.
- BrSub1006A4A0: `push &g_aBrB4FBE8; mov ecx, &g_aBrB4DF30; call`.
  C `__fastcall` dummy edx emits `mov edx,imm`. C++ 
  `((Rec6A4A0 *)g_aBrB4DF30)->go(g_aBrB4FBE8)` does not.

## Phase-cur store

`g_brPAA2904` is `(*g_ppBrPhaseCur)` (`mov ecx,[pp]; mov [ecx],edx`).
Orig is `mov eax,[PAA2948]; mov [0x10ac5c5c],eax`. Matching uses
`extern BrOptObj *DAT_10ac5c5c` (same as BrOpt3FA0).

## Slot walk

Pointer walk `add eax,0xC; cmp eax,&table[8]; jl` (signed), parallel
`inc ecx`, store `lea eax,[ecx+ecx*2]; mov [eax*4+table+4],edx`.
`(players > 1)` is `mov eax,1; cmp eax,edx; sbb edx,edx; neg edx`.
C `g_aBrAA2538[i].a` peels first id into ebp (`push ebp`). C++ does
not. C `pSlot->a` avoids ebp but loses the lea store.

## KERNEL32

IAT like BrOpt3A00: `push esi; mov esi,[__imp_GlobalHandle]; call esi`
then `FF 15` Unlock/Free. pDesc lives in esi (`xor esi,esi`).

## Residue (not source-reachable from tried C/C++)

leave_host: orig `8B 44 24 10 50` (`mov eax,[esp+0x10]; push eax`) vs
recomp `8B 4C 24 10 51` (ecx). Size-matched 490 B, 2 diffs. Named
`pA=pGame`, `int n` for AA287C, C-style f1C on leave (49 diffs, also
broke `a1/a3` DAT copy) did not flip it.

Proven against orig 0x1003CD60.bin /O2. C++ scored with cpp_score
`/O2 /GX /MD`.
